package org_bitcamp1;

public class bitc2_Boolean5 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		//논리 연산자 -> 관계식 여러 개 (관계연산자 여러개 있을때)
        int min = 1;
        int max = 10;
        int R = ??;
        boolean con1 = R <= max; //조건식 하나
        boolean con2 = R >= min;
         System.out.println(con1 && con2);        // 둘다 and
         System.out.println(con1 || con2);        // 둘다 or (이걸로는 안됨)
         
        }                               
}

